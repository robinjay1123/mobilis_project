{
  "installCommand": "git config --global --add safe.directory '*' && rm -rf flutter && git clone https://github.com/flutter/flutter.git -b 3.27.4 --depth 1 && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter pub get",
  "buildCommand": "./flutter/bin/flutter build web --release --base-href / --no-tree-shake-icons",
  "outputDirectory": "build/web",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    }
  ],
  "rewrites": [

    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}