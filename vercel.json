{
  "installCommand": "git config --global --add safe.directory '*' && if [ ! -f flutter/bin/flutter ]; then rm -rf flutter && git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && ./flutter/bin/flutter precache --web && ./flutter/bin/flutter pub get",
  "buildCommand": "export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter build web --release --base-href / --no-tree-shake-icons",
  "outputDirectory": "build/web",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}